{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capabilities: invoke app commands + listen to events",
  "windows": ["main"],
  "permissions": ["core:default"]
}
